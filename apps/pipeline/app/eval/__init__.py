"""Sprint 6b — GraphRAG vs Naive RAG vs Hybrid eval harness.

Modules:

- ``runner``  — drive the canned dataset through every retrieval mode.
- ``scoring`` — citation-recall / correctness / refusal-precision scorers.
- ``datasets/oil_gas_v1.yaml`` — canned question set (4 categories).
"""
